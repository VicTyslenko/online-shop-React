import React from 'react';
import {Box, Grid, Button, Container} from '@mui/material';

const Management = () => {
	return (<div></div>
		// <Button>
		// 	Management
		// </Button>
	);
};

export default Management;
