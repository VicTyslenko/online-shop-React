import { Container } from '@mui/material';
import { styled } from '@mui/material/styles';

export const StyledContainer = styled(Container)`
	display: flex;
	flex-direction: row;
	flex-wrap: wrap;
	gap: 5rem;
`;

export const StyledContainer1 = styled(Container)`
	display: flex;
	flex-direction: row;
	flex-wrap-wrap;
`;
